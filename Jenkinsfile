pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // komande za build projekta ovde
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // komande za testiranje projekta ovde
            }
        }
        // dodatne faze prema potrebi
    }
}
